<%= form_for([@listing, @listing.galleries.new]) do |f| %>
	<div class="field">
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</div>
	<div class="actions">
		<%= f.submit "Add" %>
	</div>
<% end %>