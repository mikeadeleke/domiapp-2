<%= div_for listing do %>
	<h3>
		<%= link_to listing.title, listing %>
		<span class="actions">
			<%= link_to "Edit", edit_listing_path(listing) %>
			<%= link_to "Delete", listing, confirm: "Do you really want to do this?", method: :delete %>
		</span>
	</h3>
<% end %>